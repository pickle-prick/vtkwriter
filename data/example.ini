; DataTypes
; Primitives: s32 u32 s64 u64 u8 s8 b8 f32 f64
; Strings: str
; Arrays: [type; count]

__HEADER__
format_version: str = "0.0.1"
key: s32 = 30000
__END_HEADER__

__BLOB__
<RAW BINARY BYTES START HERE AND CONTINUE TO EOF>
__END_BLOB__

[Information]
name: str = "robot_alpha"
total_frame_count: s32 = 1
frame_index: u32 = 0
frame_timestamp: f32 = 0.0

; [Information.Schema]
; field_names_offsets: [u32; 30] = @binary(0, 30)
; field_names: [str; 30] = @binary(0, 30)
; field_types: [s32; 30] = @binary(30, 50)

; FIXME: maybe we want to use literal syntax to make this hunman readable
[Information.Schema]
[Information.Schema.PointData]
fields: [str; 2] = ["pressure", "colors"]
fields_type: [type; 2] = [f32, f32]
fields_components: [u8; 2] = [1, 3]
[Information.Schema.CellData]
fields: [str; 2] = ["pressure", "colors"]
fields_type: [type; 2] = [f32, f32]
fields_components: [u8; 2] = [1, 3]

[Information.Meta]
last_edit: u64 = 13217891232
author: str = "Bobby"

[DataObjects:0]
type: str = "PolyData"
extent: [u32; 3] = [1, 1, 1]
block_count: u32 = 300
xml: [u8; 3000] = @binary(30, 1023)

[DataObjects:1]
type: str = "PolyData"
xml: [u8; 3000] = @binary(30, 1023)

[DataObjects:2]
type: str = "PolyData"
xml: [u8; 3000] = @binary(30, 1023)
